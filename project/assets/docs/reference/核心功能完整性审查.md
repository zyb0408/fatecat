# 核心功能完整性审查

> 日期：2026-05-07
> 范围：综合八字默认报告、紫微独立报告、纯分析内核、Web/API 交付层、未来体系边界与新导入资料资产。

## 结论

当前核心交付已经达到“仓库内可复用、可测试、可审计”的状态：综合八字默认报告和紫微独立报告边界清晰，Web/API/Bot dry-run 与导出包门禁可复现。尚不能直接宣称“生产命理正确性完备”，原因是节气、格局、用神、岁运等关键算法仍缺少外部典籍/表格的 golden 回归集，真实生产域名、CORS、token 与 Bot live smoke 也仍需外部环境验证。

## 已验证能力

| 能力 | 当前状态 | 证据 |
| --- | --- | --- |
| 综合八字 Markdown 默认报告 | 已落地 | `modules/telegram/src/report_generator.py` 的 `generate_bazi_standard_report()` |
| 紫微斗数独立报告 | 已落地 | `generate_ziwei_report()`，`test_markdown_report_api_selects_ziwei_without_bazi_blocks` |
| 体系开关 | 已落地 | `modules/telegram/src/prediction_systems.py`，`GET /api/v1/report/systems` |
| 纯分析字段裁剪 | 已落地 | `assets/fate/profiles/pure_analysis.json`，`fate_core/contracts/field_registry.py` |
| 隐私展示 | 已落地 | `test_markdown_report_hides_non_beijing_birth_place`，`scripts/check-privacy-fixtures.sh` |
| 交付门禁 | 已落地 | `scripts/acceptance.sh --with-dev`，GitHub Actions run `25454202418` |

## 本轮修正

| 问题 | 修正 |
| --- | --- |
| `pure_analysis` profile 仍允许 `huangLi`，与“出生日黄历退役为未来功能”的边界不一致 | 已从 `assets/fate/profiles/pure_analysis.json` 移除，并在 `test_field_registry.py` / `test_pure_analysis_usecase.py` 增加回归 |
| 纯分析测试里使用北京地点但保留非北京经纬度 | 已改为北京经纬度，避免示例语义漂移 |

## 主要缺口

| 缺口 | 影响 | 后续处理 |
| --- | --- | --- |
| 节气正确性缺少外部交节表 golden 对照 | 月令、司令、起运边界可能难以审计 | 使用 `assets/data/calendar/solar_terms/source_manifest.tsv` 中的表格建立 1900-2030 节气边界回归 |
| 格局、用神、调候缺少典籍规则引用层 | 结论可运行但学术依据不可追溯 | 从 `assets/data/classics/raw/bazi/` 提炼结构化规则与引用索引 |
| 遗留 `BaziCalculator` 仍保留六爻、梅花、奇门、风水等扩展分支 | 代码边界仍有历史耦合，虽然默认报告已隔离 | 后续按 `prediction_systems.py` 拆成独立 usecase/report/template |
| 未来体系仅注册为 `planned` | Web 可见但不可用 | 每个体系补输入契约、算法来源、报告模板、API/Web/Bot 测试后再启用 |
| 生产 live smoke 未完成 | 不能证明线上域名、CORS、真实 token、Bot API 可用 | 配置真实环境后运行 `scripts/production-readiness.sh --api-url <url> --require-live-bot` |

## 新导入资料资产

| 资料层 | 路径 | 状态 |
| --- | --- | --- |
| 八字典籍/讲义 raw | `assets/data/classics/raw/bazi/` | 本地私有资料，Git 与导出包排除 |
| 大六壬 raw | `assets/data/classics/raw/liuren/` | 本地私有资料，Git 与导出包排除 |
| 易占/六爻 raw | `assets/data/classics/raw/yijing/` | 本地私有资料，Git 与导出包排除 |
| 祝由 raw | `assets/data/classics/raw/zhuyou/` | 本地私有资料，Git 与导出包排除 |
| 交节时间表 raw | `assets/data/calendar/solar_terms/raw/` | 本地私有校验资料，Git 与导出包排除 |
| 资料清单 | `assets/data/classics/source_manifest.tsv`、`assets/data/calendar/solar_terms/source_manifest.tsv` | 可提交的索引；只含文件名、大小与哈希，不含个人绝对路径 |

## 下一步建议

1. 先做“节气 golden 回归”：解析 1900-2030 交节时间表，对照当前 lunar/sxwnl 输出，锁住月令与起运边界。
2. 再做“八字主线典籍引用层”：从《渊海子平》《三命通会》《子平真诠》《滴天髓》《穷通宝鉴》提炼格局、调候、用神、旺衰规则索引。
3. 最后按独立体系逐个启用黄历/择日、六爻、梅花、奇门、大六壬、风水九星、姓名合婚，禁止重新混入综合八字默认报告。
